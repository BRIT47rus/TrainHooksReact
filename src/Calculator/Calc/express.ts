

const add = (a:number,b:number)=>a+b;
const substuct = (a:number,b:number)=>a-b;
const multiply = (a:number,b:number)=>a*b;
const devide = (a:number,b:number)=>a/b;

export default {
    add,substuct,multiply,devide
}